/** 兼容入口：主题状态单一来源在 `playground/src/useTheme.ts`。 */
export {
  useTheme,
  applyCurrent,
  onActiveChange,
  commitColor,
  presets,
  presets as themePresets,
  THEME_STORAGE_KEY,
} from '../useTheme'
